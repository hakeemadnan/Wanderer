# Wanderer

A Node.js web application built with Express. This project follows an MVC structure and serves dynamic views with a clean backend setup.

## 🚀 Features
- Server-side rendering with views
- Organized MVC structure (models, views)
- Static file handling via public folder

## 📦 Installation

Clone the repository:
git clone https://github.com/hakeemadnan/Wanderer.git

Go into the project folder:
cd Wanderer

Install dependencies:
npm install

## ▶️ Run the App

npm start

Then open: http://localhost:3000

## 📁 Project Structure

- models/ → Database models  
- views/ → Templates (UI)  
- public/ → Static assets (CSS, JS)  
- app.js → Main server file  

## 🛠️ Tech Stack
- Node.js  
- Express.js  
- JavaScript
- 
